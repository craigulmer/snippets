A work-in-progress project showing how to convert airplane tracks into 
a sqlite db that Datasette can render on a map.

interesting-planes: basic info to help tag some of the planes (note
    some of the other planes that were originally in this were 
    removed since they weren't being used)

run-all.sh: Example script showing how to convert the csv into a db
